defmodule CsvParserTest do
  use ExUnit.Case
  doctest CsvParser

  # test "should return something when file exists" do
  #   assert CsvParser.read_by_from("lib/agosto_leb_full.csv", ["Nome", "E-Mail", "Cidade", "Data Faturamento"]) == %{}
  # end
end
